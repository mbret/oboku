import { saveCoverFromExternalLinkToBucket } from "./saveCoverFromExternalLinkToBucket"

it(`asd`, async () => {
  const ctx = {
    userId: `userId`
  }
  const book = {
    _id: `bookId`
  }
  const coverUrl = `https://i.nhentai.net/galleries/3147/1.jpg`

  // await saveCoverFromExternalLinkToBucket(ctx, book, coverUrl)
})